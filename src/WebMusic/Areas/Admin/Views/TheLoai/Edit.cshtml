@model TheLoai
@{ ViewData["Title"] = "Sửa thể loại"; }
<h1 class="h3 mb-3">Sửa thể loại</h1>
@if (!string.IsNullOrEmpty(Model.HinhAnh))
{
    <img src="~/images/theloai/@Model.HinhAnh" alt="" class="rounded mb-3 wm-cover-md" />
}
<div class="card wm-card-form">
    <div class="card-body">
        <form method="post" enctype="multipart/form-data">
            @Html.AntiForgeryToken()
            <input type="hidden" asp-for="MaTheLoai" />
            <div class="mb-3">
                <label class="form-label">Tên thể loại</label>
                <input asp-for="TenTheLoai" class="form-control" />
            </div>
            <div class="mb-3">
                <label class="form-label">Ảnh mới (để trống nếu giữ nguyên)</label>
                <input type="file" name="anh" accept="image/*" class="form-control" />
            </div>
            <button type="submit" class="btn btn-success">Cập nhật</button>
            <a asp-action="Index" class="btn btn-link">Hủy</a>
        </form>
    </div>
</div>