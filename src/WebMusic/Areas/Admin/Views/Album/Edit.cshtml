@model Album
@{ ViewData["Title"] = "Sửa album"; }
<h1>Sửa album</h1>
@if (!string.IsNullOrEmpty(Model.HinhAnh))
{ <img src="~/images/album/@Model.HinhAnh" alt="" class="thumb" /> }
<form method="post" enctype="multipart/form-data" class="admin-form">
    @Html.AntiForgeryToken()
    <input type="hidden" asp-for="MaAlbum" />
    <label>Tên album <input asp-for="TenAlbum" /></label>
    <label>Ảnh mới (để trống nếu giữ nguyên) <input type="file" name="anh" accept="image/*" /></label>
    <button type="submit" class="btn-primary">Cập nhật</button>
    <a asp-action="Index" class="btn-link">Hủy</a>
</form>